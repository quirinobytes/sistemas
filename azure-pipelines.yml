# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: raspberry

steps:
- script: /root/shell/push/hostexec.js centos hostname
- script: node $HOME/shell/push/release.js chatPVT
  displayName: 'Release chatPVT'

- script: |
    echo finalizado
  displayName: 'echo message'
